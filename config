[DEFAULT]
imageDir = ./files/

[VIEWER]
# How much time, in seconds, should elapse before moving to the next image
timePerPicture = 30
# How often, in minutes, should we wait between new image fetches
newPictureFetchDelay = 15

[EMAIL]
emailDomain = @gmail.com
smtpServer = imap.gmail.com
smtpPort = 993
# emailPassword = myPassword
# emailHandle = myEmailHandle
